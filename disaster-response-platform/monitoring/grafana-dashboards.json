{
  "title": "Disaster Response Metrics",
  "panels": [
    {
      "type": "graph",
      "title": "HTTP Request Duration",
      "targets": [
        {
          "expr": "rate(disaster_http_request_duration_seconds_sum[5m]) / rate(disaster_http_request_duration_seconds_count[5m])",
          "legendFormat": "Latency (s)"
        }
      ]
    }
  ]
}
